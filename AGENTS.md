# AGENTS.md

## 项目目标

FableFlow 是“寓言知识短视频”的半自动化生产系统。任何修改都应优先提升：概念准确性、故事可看性、角色一致性、视频可剪辑性、生产可复用性和数据可回流性。

## 事实来源优先级

1. `schemas/`：结构化资产硬约束。
2. `config/pipeline.json`：阶段、输入输出和人工门禁。
3. `bibles/`：世界观、角色和视觉风格长期约束。
4. `docs/`：执行规范。
5. `episodes/`：具体分集实例，不反向覆盖全局规则。

## 模型职责

- GPT-5.5：概念、故事、脚本、故事板和生成任务设计。
- gpt-image-2：角色、场景和镜头参考帧。
- Seedance：动作、运镜、光影与可选环境声。
- 剪映：旁白、字幕、节奏、字卡、混音和最终成片。

不得让 Seedance 重新设计固定角色，也不得把角色生成对白作为准确知识旁白。

## 修改规则

- 修改 Schema 时，同步更新模板、示例分集和校验脚本。
- 不得在故事前 70% 直接出现概念名称或明显定义。
- 长期角色变更先更新 `bibles/character-bible.md`。
- 长期画风变更先更新 `bibles/style-bible.md`。
- 示例分集必须通过 `python tools/validate.py`。
- 不提交大体积图片、音频和视频成品；默认只提交说明文件或小尺寸审核样张。
- 所有 AI 输出均视为草稿，必须经过对应人工 Gate。
- Seedance 片段连续失败两次时，优先降低复杂度或静帧降级，不无限重试。

## 内容语言

内部生产资产默认使用中文；JSON 字段名、文件名和程序代码使用英文。
