# AGENTS.md

## 项目目标

FableFlow 是“寓言知识短视频”的半自动化生产系统。任何修改都应优先提升：概念准确性、故事可看性、角色一致性、生产可复用性和数据可回流性。

## 事实来源优先级

1. `schemas/`：结构化资产的硬约束。
2. `config/pipeline.json`：阶段顺序、输入输出和人工门禁。
3. `bibles/`：世界观、角色、视觉风格的长期约束。
4. `docs/`：执行规范。
5. `episodes/`：具体分集实例，不反向覆盖全局规则。

## 修改规则

- 修改 Schema 时，同步更新模板、示例分集和校验脚本。
- 不得在故事前 70% 直接出现概念名称或明显定义。
- 角色外观发生长期变更时，必须先更新 `bibles/character-bible.md`。
- 画风发生长期变更时，必须先更新 `bibles/style-bible.md`。
- 示例分集必须通过 `python tools/validate.py`。
- 不提交生成的大体积图片、音频和视频成品；默认只提交说明文件或小尺寸审核样张。
- 所有 AI 输出均视为草稿，必须经过对应人工 Gate。

## 内容语言

内部生产资产默认使用中文；JSON 字段名、文件名和程序代码使用英文。
