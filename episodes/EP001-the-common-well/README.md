# EP001｜多打一桶水

- 概念：公地悲剧
- 状态：示例基线，尚未生成正式图片与视频
- 目标时长：100 秒
- 故事方向：公共水井

## 执行顺序

1. 审核 `episode-brief.json` 和 `concept-sheet.json`。
2. 查看三个方向并读取 `decision.json`。
3. 以 `script.json` 和 `storyboard.json` 为成片依据。
4. 按 `asset-manifest.json` 顺序生成关键图。
5. 执行 `make_srt.py` 和 `make_edit_list.py`。
6. 在剪映中装配并通过 G4。
